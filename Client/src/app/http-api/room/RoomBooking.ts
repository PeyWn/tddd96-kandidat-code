import {BookedRoom} from './BookedRoom';

export class RoomBooking {
  id: number;
  preliminary: boolean;
  updatedAt: string;
  createdAt: string;
  DecisionId: number;
  UserId: number;

  Booked_local: BookedRoom;
}
